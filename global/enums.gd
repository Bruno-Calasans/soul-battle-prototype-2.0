extends Node

enum CARD_TYPE {
	CREATURE,
	SPELL,
	STRUCTURE
}


enum CARD_RARITY {
	COMMON,
	LEGENDARY,
	DIVINE
}


enum CREATURE_SKILL_TYPE {
	PASSIVE,
	ACTIVE,
	ULTIMATE
}


enum ELEMENT {
	WATER,
	FIRE,
	EARTH,
	AIR,
	ELETRICITY,
	LIGHT,
	DARK,
	ICE
}


enum DMG_TYPE {
	DIRECT,
	PHYSICAL,
	EARTH,
	WIND,
	FIRE,
	WATER,
	ICE,
	LIGHT,
	DARK,
	AIR,
	ELETRICITY
}


enum CREATURE_RACE {
	FISH,
	HUMAN,
	DEMON,
	ANGEL,
	MACHINE,
	ELEMENTAL,
	INSECT,
	ANIMAL,
	UNDEAD,
	BEAST,
	DRAGON,
}

enum CREATURE_CLASS {
	MYSTICAL,
	SKILL,
	BEAST,
}

enum EFFECT_TYPE {
	PASSIVE,
	ACTIVE,
	ULTIMATE,
	SPELL,
	STRUCTURE,
	KEYWORD,
	AURA
}

enum SIDE {
	SELF,
	ALLY,
	ENEMY,
	ANY
}

enum FILTER_SUBJECT {
	EVENT,
	CARD,
	CREATURE,
	SPELL,
	STRUCTURE,
	PLAYER,
	GAME
}

enum FILTER_FIELD {
	ATTACK,
	HEALTH,
	DEF,
	EVADE,
	OWNER,
	RACE,
	CLASS,
	ENERGY,
	SOUL,
	LIFE,
	SOURCE,
	TARGET
}

enum COMPARATOR {
	BELOW_TO,
	ABOVE_TO,
	EQUALS_TO,
	NOT_EQUALS_TO,
	BELOW_OR_EQUALS_TO,
	ABOVE_OR_EQUALS_TO,
	CONTAINS,
	NOT_CONTAINS
}

enum OPERATION {
	INCREASE,
	DECREASE,
	SET
}

enum EFFECT_TRIGGER_TIMING {
	BEFORE,
	AFTER,
}

enum GROUP {
	ALL,
	ONE,
	RANDOM,
}

enum ACTION_TYPE {
	DAMAGE,
	HEAL,
	DESTROY,
	MOVE,
	DRAW,
	MODIFY_STATS,
	APPLY_BUFF,
	APPLY_DEBUFF,
	SUMMON,
	DIRECT_ATTACK,
	BATTLE_ATTACK,
	DEMOLITION_ATTACK
}

enum COMMAND_TYPE {
	ATTACK,
	SUMMON,
}

enum GAME_EVENT {
	ATTACK,
	DESTROY,
	DAMAGE,
	TURN_START,
	TURN_END,
	DRAW,
	SUMMON,
	MOVE,
	BEFORE_ACTIVE_EFFECT,
	AFTER_ACTIVE_EFFECT,
	APPLY_STATUS_EFFECT,
	REMOVE_STATUS_EFFECT,
	CANCEL_GAME_COMMAND,
	CANCEL_ACTION
}

enum ATTRIBUTE {
	HP,
	ATTACK,
	DEF,
	EVADE,
}

enum ZONE_TYPE {
	DECK,
	HAND,
	VOID,
	BOARD
}

enum RULE_MODIFIER {
	NO_ATTACK,
	NO_HEAL,
	NO_TARGET
}

enum EVENT_FIELD {
	TARGET, # fora de event.data
	SOURCE, # fota de event.data
	VALUE, # dentro de event.data
	DMG_TYPE, # dentro de event.data
	SLOT # dentro de event.data
}


enum STATUS_EFFECT_COMPONENT {
	TRIGGER, # REAGE À EVENTOS
	MODIFIER, # MODIFICA STATUS DO CARD
	RULE_CHANGE # MUDA REGRAS. EX.: NÃO ATACAR, NÃO ATIVAR HABILIDADE
}

enum SLOT_POSITION {
	LEFT,
	MIDDLE,
	RIGHT,
}

# pontos de interceptação
# verifica se alguém quer fazer uma mudança
enum RULE_HOOK {
	CAN_EXECUTE_ACTION,
	CAN_BE_TARGETED,
	BEFORE_ATTACK,
	ON_ATTACK,
	AFTER_ATTACK,
	BEFORE_DAMAGE,
	AFTER_DAMAGE,
	BEFORE_APPLY_STATUS_EFFECT
}

enum STATUS_EFFECT_TYPE {
	PERMANENT,
	PERIODIC,
	AURA
}
